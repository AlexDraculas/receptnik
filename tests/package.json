{
  "name": "cookly-tests",
  "private": true,
  "type": "module",
  "description": "Node test suites for the Cookly worker + full-stack integration behavior. Run with 'npm test' from this folder (see .github/workflows/test.yml for CI).",
  "scripts": {
    "test:worker": "node worker-test/test.mjs",
    "test:frontend-unit": "node frontend-unit-test/run.mjs",
    "test:integration": "node integration-test/run.mjs",
    "test": "npm run test:worker && npm run test:frontend-unit && npm run test:integration"
  },
  "dependencies": {
    "jsdom": "^30.0.1"
  }
}
